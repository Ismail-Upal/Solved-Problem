#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<math.h>
int my_len(char s[], int n)
{
    int cnt=0;
    for(int i=0; i<'\0'; i++){
        cnt++;
    }
    return cnt;
}
int main()
{
    char s[];
    fgets(sizeof(s), %s, s);
    int ans=my_len(s,n);
    printf("%d", ans);
    return 0;
}